import { SearchFilterPipe } from './search-filter.pipe';

describe('SearchFilterPipe', () => {
  it('create an instance', () => {
    const searchFilter = new SearchFilterPipe();
    expect(searchFilter).toBeTruthy();
  });
});
